# ml_model.py

import random

def run_method2(news: dict) -> float:
    """
    머신러닝 기반 분석 (여기서는 랜덤 값 예시)
    실제로는 학습된 모델 불러오기 -> 예측 점수 출력
    """
    # 예시: 0.0 ~ 1.0 사이 점수 반환
    return round(random.uniform(0.4, 0.9), 3)
